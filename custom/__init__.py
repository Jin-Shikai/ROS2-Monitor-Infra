"""User-supplied DataConverter / VerdictService implementations.

Files in this directory are meant to be copied and modified. The framework
itself ships only abstract base classes (`DataConverter`, `VerdictService`)
and the transport plumbing (`Dispatcher`, `ConverterExporter`,
`VerdictExporter`). Concrete DSL logic lives here, outside the framework.
"""
