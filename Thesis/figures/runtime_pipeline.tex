\begin{tikzpicture}[
  font=\scriptsize,
  runtime/.style={draw, fill=white, rounded corners=2pt,
                  align=center, minimum height=10mm, minimum width=22mm},
  plugin/.style={runtime, dashed},
  flow/.style={-{Latex[length=2mm]}, line width=.7pt}
]
\node[runtime] (source) {Collector or\\transport source};
\node[runtime, right=6mm of source] (transformer) {Transformer\\pipeline};
\node[runtime, right=6mm of transformer] (dispatcher) {Record\\dispatcher};
\node[plugin, right=6mm of dispatcher] (converter) {Converter\\plug-in};
\node[plugin, right=6mm of converter] (verdict) {Verdict-service\\plug-in};
\node[runtime, right=6mm of verdict] (exporter) {Verdict\\exporter};
\foreach \a/\b in {source/transformer,transformer/dispatcher,dispatcher/converter,
                    converter/verdict,verdict/exporter}
  \draw[flow] (\a) -- (\b);
\end{tikzpicture}
