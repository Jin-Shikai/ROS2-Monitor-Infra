\begin{tikzpicture}[
  font=\scriptsize,
  actor/.style={draw, fill=white, rounded corners=2pt,
                align=center, minimum width=24mm, minimum height=8mm},
  msg/.style={-{Latex[length=2mm]}, line width=.6pt},
  life/.style={draw=black!45, dashed}
]
\node[actor] (sum) {ROS 2 source};
\node[actor, right=10mm of sum] (monitor) {Monitor runtime};
\node[actor, right=10mm of monitor] (verifier) {Verifier runtime};
\node[actor, right=10mm of verifier] (output) {Evidence output};

\foreach \n in {sum,monitor,verifier,output}
  \draw[life] (\n.south) -- ++(0,-52mm);

\draw[msg] ($(sum.south)+(0,-8mm)$) -- node[above]{1. typed observation}
  ($(monitor.south)+(0,-8mm)$);
\draw[msg] ($(monitor.south)+(0,-19mm)$) -- node[above]{2. common record}
  ($(verifier.south)+(0,-19mm)$);
\draw[msg] ($(verifier.south)+(0,-30mm)$) -- node[above]{3. verdict}
  ($(output.south)+(0,-30mm)$);
\draw[msg] ($(monitor.south)+(0,-41mm)$) -- node[above]{record/evidence}
  ($(output.south)+(0,-41mm)$);

\node[align=left, anchor=west] at ($(monitor.south)+(2mm,-48mm)$)
  {In an integrated deployment, steps 1--3 occur inside one runtime.};
\end{tikzpicture}
