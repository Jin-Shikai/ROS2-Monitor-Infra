% UML class model of the deployment specification (redrawn from CodeGen.png;
% root element renamed GenerationRequest -> DeploymentSpecification).
\begin{tikzpicture}[
  font=\scriptsize,
  class/.style={rectangle split, rectangle split parts=2, draw,
                rectangle split part fill={black!4,white},
                align=center, inner xsep=7pt, inner ysep=4pt},
  attr/.style={align=left},
  comp/.style={{Diamond[fill=black,length=2.8mm,width=1.7mm]}-, semithick},
  gen/.style={-{Triangle[open,length=3.2mm,width=3.4mm]}, semithick},
  plain/.style={semithick},
  mult/.style={font=\scriptsize, inner sep=1.5pt}
]

% ---------------- row 0: root ----------------
\node[class] (ds) at (13,0)
  {\textbf{DeploymentSpecification}
   \nodepart{two}\begin{tabular}{@{}l@{}}
     + hosts: [host]\\ + links: [link]\end{tabular}};

% ---------------- row 1 ----------------
\node[class] (host) at (3,-3.2)
  {\textbf{Host}
   \nodepart{two}\begin{tabular}{@{}l@{}}
     + id\\ + runtimes: [runtime]\end{tabular}};
\node[class] (runtime) at (13,-3.2)
  {\textbf{Runtime}
   \nodepart{two}\begin{tabular}{@{}l@{}}
     + id\\ + kind: ros2 $|$ monitor $|$ converter $|$ verdict\_service
   \end{tabular}};
\node[class] (link) at (23,-3.2)
  {\textbf{Link}
   \nodepart{two}\begin{tabular}{@{}l@{}}
     + id\\ + payload: records $|$ dsl\\ + transport\end{tabular}};

% ---------------- row 2: runtime kinds and transport ----------------
\node[class] (po) at (2.6,-7.0)
  {\textbf{Process Observables}
   \nodepart{two}\begin{tabular}{@{}l@{}}+ sources: [source]\end{tabular}};
\node[class] (mon) at (7.4,-7.0)
  {\textbf{Monitor}
   \nodepart{two}\begin{tabular}{@{}l@{}}+ subscribe: [source]\end{tabular}};
\node[class] (conv) at (12.4,-7.0)
  {\textbf{Converter}
   \nodepart{two}\begin{tabular}{@{}l@{}}+ converter\\ + class\_path\end{tabular}};
\node[class] (vs) at (17.2,-7.0)
  {\textbf{VerdictService}
   \nodepart{two}\begin{tabular}{@{}l@{}}+ class\_path\end{tabular}};
\node[class] (transport) at (23,-7.0)
  {\textbf{Transport}
   \nodepart{two}\begin{tabular}{@{}l@{}}
     + kind: TransportKind\\ + parameter\end{tabular}};

% ---------------- row 3 ----------------
\node[class] (source) at (2.6,-10.6)
  {\textbf{Source}
   \nodepart{two}\begin{tabular}{@{}l@{}}
     + id\\ + source\_kind: SourceKind\\ + name\\ + interface\end{tabular}};
\node[class] (sourcekind) at (6.8,-10.6)
  {\textbf{SourceKind}
   \nodepart{two}\begin{tabular}{@{}l@{}}topic\\ service\\ action\end{tabular}};
\node[class] (datafilter) at (10.9,-10.6)
  {\textbf{DataFilter}
   \nodepart{two}\begin{tabular}{@{}l@{}}+ converter: data\_filter\end{tabular}};
\node[class] (dslconv) at (15.2,-10.6)
  {\textbf{DslConverter}
   \nodepart{two}\begin{tabular}{@{}l@{}}+ converter: dsl\_converter\end{tabular}};
\node[class] (transportkind) at (23,-10.6)
  {\textbf{TransportKind}
   \nodepart{two}\begin{tabular}{@{}l@{}}mqtt\\ file\end{tabular}};

% ---------------- compositions from the root ----------------
\draw[comp] (ds.west) -| (host.north)
  node[mult, pos=0.96, left=2pt] {0..*};
\draw[comp] (ds.east) -| (link.north)
  node[mult, pos=0.96, right=2pt] {0..*};

% ---------------- host / runtime / link associations ----------------
\draw[comp] (host.east) -- (runtime.west)
  node[mult, pos=0.06, above] {1} node[mult, pos=0.94, above] {0..*};
\draw[comp] (runtime.east) -- (link.west)
  node[mult, pos=0.06, above] {1} node[mult, pos=0.94, above] {0..*};

% ---------------- runtime generalization ----------------
\coordinate (rbus) at (13,-5.4);
\draw[gen] (rbus) -- (runtime.south);
\draw[plain] (po.north) |- (rbus);
\draw[plain] (mon.north) |- (rbus);
\draw[plain] (conv.north) |- (rbus);
\draw[plain] (vs.north) |- (rbus);

% ---------------- converter generalization ----------------
\coordinate (cbus) at (12.4,-9.0);
\draw[gen] (cbus) -- (conv.south);
\draw[plain] (datafilter.north) |- (cbus);
\draw[plain] (dslconv.north) |- (cbus);

% ---------------- remaining associations ----------------
\draw[comp] (po.south) -- (source.north);
\draw[plain] (source.east) -- (sourcekind.west);
\draw[comp] (link.south) -- (transport.north);
\draw[plain] (transport.south) -- (transportkind.north);

\end{tikzpicture}
