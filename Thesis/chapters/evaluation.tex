\chapter{Evaluation}\label{chapter:evaluation}

This chapter evaluates the implemented infrastructure and answers RQ4:

\begin{quote}
\textbf{RQ4:} To what extent does the implemented infrastructure support the
monitoring needs of realistic ROS~2 applications?
\end{quote}

The evaluation uses five experiments. E1 starts with one topic and an
integrated monitor. E2 adds services, actions, and a split deployment over
MQTT. E3 applies the infrastructure to an unmodified Nav2 application. E4
extends the same setup to two robots and a property that uses observations
from both robots. E5 places the robots, monitor, and verifier on three
different physical machines. Together, the experiments cover the main
capabilities introduced in Chapters~\ref{chapter:design}
and~\ref{chapter:implementation} and provide complementary evidence for RQ4.

\begin{table}[htbp]
\centering
\begin{tabular}{
    >{\raggedright\arraybackslash}p{0.08\textwidth}
    >{\raggedright\arraybackslash}p{0.27\textwidth}
    >{\raggedright\arraybackslash}p{0.25\textwidth}
    >{\raggedright\arraybackslash}p{0.29\textwidth}
}
\toprule
\textbf{ID} & \textbf{Application} & \textbf{Deployment} &
\textbf{Main purpose} \\
\midrule
E1 & Commanded-speed stimulus & Integrated & Check topic collection and local
verdict generation. \\
E2 & Speed, reset service, and Fibonacci action stimuli & Split over MQTT &
Check topics, services, and actions, and compare local and remote verdicts. \\
E3 & One TurtleBot3 using Nav2 in Gazebo & Split over MQTT & Check a realistic
navigation application with changing routes and action outcomes. \\
E4 & Two TurtleBot3 robots using Nav2 & Split over MQTT & Check namespaced
robot data and a minimum-separation property using both robots. \\
E5 & The E4 application on three computers & Split over DDS and MQTT on a LAN
& Check physical distribution across Ubuntu, Raspberry Pi, and macOS. \\
\bottomrule
\end{tabular}
\caption{Overview of the five evaluation experiments.}
\label{tab:eval-overview}
\end{table}

\section{Evaluation Method}
\label{sec:eval-method}

E1--E4 ran on the same Ubuntu~24.04 workstation with ROS~2 Kilted. The robot
experiments used Gazebo and Nav2. These MQTT experiments used a local
Mosquitto broker on port~1884. E5 used the same workstation for Gazebo and
Nav2, an Ubuntu~24.04 Raspberry Pi for collection and the broker, and a macOS
laptop for verification. Gazebo and RViz were used to inspect the robot
experiments and make the route changes, robot crossings, and physical
three-machine setup visible;
the tables are calculated from the recorded experiment artifacts.

The terms \emph{integrated} and \emph{split} describe process placement. 
E1 checks properties inside the monitor
process. In E2--E4, the monitor and the verifier are separate
processes, but they still run on the same machine and exchange records
through an MQTT broker on \texttt{localhost}. This setup checks whether the
two runtime roles produce identical verdicts when separated.
E5 uses the same split but
places the ROS~2 application, monitor, and verifier on three computers. DDS
carries the robot topics from the workstation to the Pi, and MQTT carries the
resulting data records from the Pi to the laptop. Verification runs on the
laptop independently of ROS~2.

\section{E1: Topic Monitoring in an Integrated Deployment}
\label{sec:eval-e1}

E1 establishes the simplest working path through the infrastructure.

\paragraph{Deployment.} One physical workstation runs two ROS~2 processes: a 
node and the monitor. The collector, converter, and verdict service
all run inside the monitor process, which contains every runtime role in E1.

\paragraph{Sources and expected observations.}
Table~\ref{tab:eval-e1-sources} lists the only source in this experiment.

\begin{table}[htbp]
\centering
\begin{tabular}{
    >{\raggedright\arraybackslash}p{0.29\textwidth}
    >{\raggedright\arraybackslash}p{0.14\textwidth}
    >{\raggedright\arraybackslash}p{0.47\textwidth}
}
\toprule
\textbf{Source} & \textbf{Kind} & \textbf{Expected observations} \\
\midrule
\texttt{/cmd\_vel} & Topic & One message every 0.2 seconds.
\texttt{linear.x} alternates between $1.85$~m/s and $0.2$~m/s every three
seconds. Each change across the $0.5$~m/s limit should produce one verdict. \\
\bottomrule
\end{tabular}
\caption{Sources and expected observations in E1.}
\label{tab:eval-e1-sources}
\end{table}

\paragraph{Rules.} The monitor converts the speed field and checks the
following rule:

\[
    \texttt{linear.x} \leq 0.5\ \text{m/s}.
\]

The verdict is negative when the speed first exceeds the limit and positive
when it returns below the limit. Verdicts occur only when the property state
changes.

\paragraph{Results.}
The reference run lasted 120 seconds. It contained 593 topic records and 40
changes between the satisfied and violated states. The monitor produced the
same 40 results. Every result referred to the first data record on the new
side of the threshold, and the topic sequence was complete.

\begin{table}[htbp]
\centering
\begin{tabular}{lr}
\toprule
\textbf{Check} & \textbf{Result} \\
\midrule
Collected \texttt{/cmd\_vel} records & 593 \\
Expected / produced verdicts & 40 / 40 \\
Missed or additional verdicts & 0 \\
\bottomrule
\end{tabular}
\caption{Results of E1.}
\label{tab:eval-e1}
\end{table}

\paragraph{Finding.} E1 shows that the basic topic path works as intended. The
collector, converter, and verdict service preserve the expected sequence of
property changes.

\section{E2: Topics, Services, and Actions in a Split Deployment}
\label{sec:eval-e2}

E2 separates collection from checking while adding service and action
observations.

\paragraph{Deployment.} All components still run on one physical workstation,
but in separate processes. Three ROS~2 stimulus processes
publish the test data. A monitor process collects their observations through
DDS. It writes the records locally and sends them to a Mosquitto process. A
separate verifier process receives the records from MQTT on
\texttt{localhost}. The speed rule runs both inside the monitor and inside the
verifier; the reset rule runs in the verifier.

\paragraph{Sources and expected observations.}
Table~\ref{tab:eval-e2-sources} lists all four monitored sources.

\begin{table}[htbp]
\centering
\begin{tabular}{
    >{\raggedright\arraybackslash}p{0.29\textwidth}
    >{\raggedright\arraybackslash}p{0.14\textwidth}
    >{\raggedright\arraybackslash}p{0.47\textwidth}
}
\toprule
\textbf{Source} & \textbf{Kind} & \textbf{Expected observations} \\
\midrule
\texttt{/cmd\_vel} & Topic & The same 5~Hz alternating speed
sequence as E1. Local and remote checks should produce identical verdicts. \\
\texttt{/reset\_pose} & Service & One request and response
every three seconds. Every response reports success. Odd calls should reset
the pose; even calls deliberately leave it unchanged. \\
\texttt{/odom} & Topic & Pose updates at 5~Hz. After odd
reset calls, the next positions should return close to the origin. After even
calls, they should remain away from the origin. \\
\texttt{/fibonacci} & Action & One order-6 goal every five
seconds. Each completed goal should produce five feedback observations and a
\textsc{succeeded} status. \\
\bottomrule
\end{tabular}
\caption{Sources and expected observations in E2.}
\label{tab:eval-e2-sources}
\end{table}

\paragraph{Rules.} The monitor and verifier check the following speed rule:

\[
    \texttt{linear.x} \leq 0.5\ \text{m/s}.
\]

The verifier also checks the following reset rule:

\[
    \text{distance to origin after a successful reset}
    \leq 0.05\ \text{m within 1 second}.
\]

The speed rule is checked twice from the same records, so the local and remote
verdict streams should be identical. The reset rule uses a service-response
record and a later odometry record. The Fibonacci feedback and final status
serve as the expected evidence for checking action collection.

\paragraph{Results.}
In the 120-second reference run, the monitor collected 1458 records from the
four sources. The local and remote speed checks each produced 40 verdicts,
with the same result and contributing record identifiers in every case. The
reset rule produced 39 alternating positive and negative verdicts, as expected
from the test node. The action collector recorded all feedback and final
statuses for 23 goals.

\begin{table}[htbp]
\centering
\begin{tabular}{
    >{\raggedright\arraybackslash}p{0.55\textwidth}
    >{\raggedright\arraybackslash}p{0.34\textwidth}
}
\toprule
\textbf{Check} & \textbf{Result} \\
\midrule
Local and remote speed verdicts & 40 / 40 identical \\
Remote speed verdicts compared with recorded input & 40 / 40 correct \\
Reset-service verdicts & 39, alternating as expected \\
Reset verdict attribution & Service response and odometry present in every
verdict \\
Fibonacci actions & 23 goals, 115 feedback records, 23 successful statuses \\
\bottomrule
\end{tabular}
\caption{Results of E2.}
\label{tab:eval-e2}
\end{table}

\paragraph{Finding.} E2 shows that the same records produce identical verdicts
when checked locally or after transport over MQTT. It also shows that
topic, service, and action observations can be collected in one run, and that
records from a service and a topic can contribute to one result.

\section{E3: Monitoring a Nav2 Application}
\label{sec:eval-e3}

E3 replaces the small test nodes with an unmodified Nav2 application. A
TurtleBot3 moves between the left and right sides of a corridor map in Gazebo.
The purpose is to check whether the infrastructure can follow real Nav2 topic
and action data while the available route and action outcome change.

\paragraph{Deployment.} One physical workstation runs Gazebo, Nav2, the
mission process, the monitor, Mosquitto, and the verifier. Nav2 and the monitor
are separate ROS~2 processes connected through DDS. The monitor and verifier
are also separate processes and exchange records through MQTT on
\texttt{localhost}. The processes form a split deployment on the same machine.

\paragraph{Sources and expected observations.}
Table~\ref{tab:eval-e3-sources} lists the three monitored Nav2 sources.

\begin{table}[H]
\centering
\begin{tabular}{
    >{\raggedright\arraybackslash}p{0.29\textwidth}
    >{\raggedright\arraybackslash}p{0.14\textwidth}
    >{\raggedright\arraybackslash}p{0.47\textwidth}
}
\toprule
\textbf{Source} & \textbf{Kind} & \textbf{Expected observations} \\
\midrule
\texttt{/cmd\_vel} & Topic & Nav2 velocity commands
during motion and stopping. Every change across $0.2$~m/s should produce the
corresponding speed verdict. \\
\texttt{/odom} & Topic & Robot pose and velocity while the
mission runs. Only the required fields should be retained and the stream
limited to 5~Hz. \\
\texttt{/navigate\_to\_pose} & Action & Feedback and
status for three goals. A goal should receive a positive verdict only if its
final status is \textsc{succeeded} within 35 seconds. A late, cancelled, or
aborted goal should receive a negative verdict. \\
\bottomrule
\end{tabular}
\caption{Sources and expected observations in E3.}
\label{tab:eval-e3-sources}
\end{table}

\paragraph{Rules.} The verifier checks the following speed rule:

\[
    \texttt{linear.x} \leq 0.2\ \text{m/s}.
\]

It also checks the following navigation rule:

\[
    \text{goal status} = \textsc{succeeded}
    \quad\text{and}\quad
    \text{duration} \leq 35\ \text{s}.
\]

A goal receives a negative verdict if it exceeds the time limit, is
cancelled, or ends with an aborted status. Odometry is reduced to the required
fields and limited to 5~Hz before it is stored and sent to the verifier.

\paragraph{Mission.} The mission contains three goals. Goal~A uses the normal
lower route through the right-hand partition. Before goal~B, one obstacle
closes the complete lower opening, so the robot must return through the upper
opening. Before goal~C, a second obstacle closes the upper opening while the
first remains in place. The two sides of the map are then disconnected.
Figures~\ref{fig:eval-e3-goals-a-b} and~\ref{fig:eval-e3-goal-c} show this
progression and the verdict log produced as each goal is decided.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.82\textwidth]{figures/evaluation/e3_a.png}\\[-1mm]
{\small (a) Goal A uses the open lower route and succeeds.}

\smallskip
\begin{minipage}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/evaluation/e3_b.png}\\[-1mm]
{\small (b) Goal B follows the upper route.}
\end{minipage}\hfill
\begin{minipage}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/evaluation/e3_b_fails.png}\\[-1mm]
{\small (c) The verifier reports the 35-second violation.}
\end{minipage}
\caption{The first two E3 goals. (a) Goal A reaches the right side through the
lower opening. After the red obstacle closes that opening, (b) goal B takes
the upper route and (c) remains active beyond the deadline. The additional
\texttt{result: false} entry in the verifier terminal is the online verdict
for goal B.}
\label{fig:eval-e3-goals-a-b}
\end{figure}

\begin{figure}[htbp]
\centering
\begin{minipage}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/evaluation/e3_c_start.png}\\[-1mm]
{\small (a) Goal C starts with both openings closed.}
\end{minipage}\hfill
\begin{minipage}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/evaluation/e3_c_fails.png}\\[-1mm]
{\small (b) Nav2 aborts and the verifier reports failure.}
\end{minipage}
\caption{Goal C in E3. Both red obstacles remain in the central partition, so
the robot remains on the left side. Nav2 ends the action with an aborted
status, and the last line in the verifier terminal is the corresponding
negative verdict.}
\label{fig:eval-e3-goal-c}
\end{figure}

\paragraph{Results.} The route was checked from the Gazebo position log.
During goal~A, the robot crossed the partition at $y=-1.287$~m. During goal~B,
it crossed at $y=1.055$~m, confirming that closing the lower opening changed
the route. Goal~C remained on the left side of the partition.

Goal~A succeeded in 24.34 seconds and therefore satisfied the 35-second rule.
Goal~B eventually succeeded, but required 69.72 seconds. The verifier emitted
its negative verdict as soon as the deadline passed while the action was
still active. Goal~C was aborted by Nav2 after 33.10 seconds, producing the
third verdict immediately from the observed action status. The independently
recorded mission log confirmed all three results.

\begin{table}[htbp]
\centering
\begin{tabular}{
    >{\raggedright\arraybackslash}p{0.18\textwidth}
    >{\raggedright\arraybackslash}p{0.21\textwidth}
    >{\raggedright\arraybackslash}p{0.24\textwidth}
    >{\raggedright\arraybackslash}p{0.25\textwidth}
}
\toprule
\textbf{Goal} & \textbf{Nav2 result} & \textbf{Route} &
\textbf{Navigation-rule verdict} \\
\midrule
A & Succeeded in 24.34~s & Lower opening & Positive \\
B & Succeeded in 69.72~s & Upper opening & Negative; time limit exceeded \\
C & Aborted in 33.10~s & Stayed on left side & Negative; aborted \\
\bottomrule
\end{tabular}
\caption{Navigation results and verdicts in E3.}
\label{tab:eval-e3-goals}
\end{table}

The run contained 15,833 records: 2422 velocity records, 12,720 action
records, and 691 odometry records after rate limiting. The speed rule produced
12 verdicts, all matching the recorded velocity input. The navigation rule
produced one verdict for each goal, and all three matched the recorded action
outcome and duration.

\paragraph{Finding.} E3 shows that the infrastructure can monitor a realistic
ROS~2 application while Nav2 remains unchanged. The three goals exercise the
complete navigation rule: success before the deadline, a goal still running
at the deadline, and an explicit Nav2 failure. In each case the online verdict
agreed with the route and action logs recorded independently.

\section{E4: Monitoring Two Nav2 Robots}
\label{sec:eval-e4}

E4 uses two namespaced TurtleBot3 and Nav2 instances. The robots start on
opposite sides of the map, move past each other, and then return. The purpose
is to check that observations remain associated with the correct robot and
that one rule can combine positions from both namespaces.

\paragraph{Deployment.} All components again run on one physical workstation.
Gazebo simulates both robots, and two namespaced Nav2 stacks run as separate
ROS~2 components. One monitor collects both namespaces through DDS. The
monitor sends records through a local Mosquitto process to one verifier
process. The verifier contains two independent speed checks and one check that
uses positions from both robots. As in E2 and E3, MQTT separates processes over
the workstation's loopback network.
Figure~\ref{fig:eval-e4-deployment} summarizes this placement and the two
communication steps.

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{\input{diagrams/e4_deployment.tex}}
\caption{Deployment of E4. Gazebo, both namespaced Nav2 stacks, the monitor,
the local MQTT broker, and the verifier are separate processes on one Ubuntu
workstation. DDS carries the six ROS~2 topic streams to the monitor; MQTT
carries the resulting data records to the verifier.}
\label{fig:eval-e4-deployment}
\end{figure}

\paragraph{Sources and expected observations.}
E4 monitors six topic sources and focuses on multi-robot topic correlation;
E2 and E3 already exercised action collection.

\begin{table}[htbp]
\centering
\begin{tabular}{
    >{\raggedright\arraybackslash}p{0.29\textwidth}
    >{\raggedright\arraybackslash}p{0.14\textwidth}
    >{\raggedright\arraybackslash}p{0.47\textwidth}
}
\toprule
\textbf{Source} & \textbf{Kind} & \textbf{Expected observations} \\
\midrule
\texttt{/robot1/cmd\_vel},
\texttt{/robot2/cmd\_vel} & Topic & Commanded speed
for each robot. Each namespace should feed only its own $0.3$~m/s speed check,
and every threshold change should produce one verdict. \\
\texttt{/robot1/amcl\_pose},
\texttt{/robot2/amcl\_pose} & Topic & Localized positions in the shared map
frame. Their distance should fall below 1.0~m and later recover during each of
the two crossings, producing two violation and two recovery verdicts. \\
\texttt{/robot1/odom},
\texttt{/robot2/odom} & Topic & Pose and velocity in each
robot's odometry frame, reduced to the required fields and limited to 5~Hz.
These sources are recorded for inspection, while the separation rule uses the
localized poses. \\
\bottomrule
\end{tabular}
\caption{Sources and expected observations in E4.}
\label{tab:eval-e4-sources}
\end{table}

\paragraph{Rules.} The verifier checks the following speed rule separately for
each robot:

\[
    \texttt{linear.x} \leq 0.3\ \text{m/s}.
\]

It also checks the following separation rule:

\[
    \text{distance between the two robots} \geq 1.0\ \text{m}.
\]

The separation rule uses the localized poses because both positions are then
expressed in the shared map frame. Each new pose is paired with the latest pose
of the other robot. A negative verdict is produced when the distance first
falls below 1.0~m, and a positive verdict is produced when it rises above the
limit again. Gazebo positions are recorded separately so that this result can
also be compared with the simulated physical distance.

\paragraph{Mission.} The robots start on opposite sides of the map. Each robot
navigates to the other side and then returns to its starting side, producing
two crossings and four completed navigation goals.
Figure~\ref{fig:eval-e4-crossings} shows both namespaced RViz views and the two
physical crossings in Gazebo.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.84\textwidth]{figures/evaluation/e4_meet_rviz.png}\\[-1mm]
{\small (a) Independent \texttt{/robot1} and \texttt{/robot2} RViz views.}

\smallskip
\begin{minipage}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/evaluation/e4_meet_first.png}\\[-1mm]
{\small (b) First crossing.}
\end{minipage}\hfill
\begin{minipage}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/evaluation/e4_meet_second.png}\\[-1mm]
{\small (c) Return crossing.}
\end{minipage}
\caption{The E4 two-robot mission. The two RViz windows show that each Nav2
stack keeps its own pose, path, and costmap. Gazebo shows the corresponding
physical robot positions during the first and second crossings, where the
minimum-separation rule is violated and later recovers.}
\label{fig:eval-e4-crossings}
\end{figure}

\paragraph{Results.} All four navigation goals succeeded. The monitor
collected 3463 records. The two speed checks produced 12 and 20 verdicts
respectively; every verdict matched the recorded command stream for that
robot. The separation check produced two violation periods and two
corresponding recovery verdicts. All four transitions matched the replay of
the localized pose records, including the contributing record identifiers.

The smallest distance calculated from the localized poses was $0.587$~m. The
smallest physical distance reported by Gazebo was $0.623$~m. Both physical
periods below 1.0~m overlapped with the online violation periods, with exactly
two online periods produced. The four physical goal positions were between
$0.050$~m and $0.073$~m from their requested positions.

\begin{table}[htbp]
\centering
\begin{tabular}{
    >{\raggedright\arraybackslash}p{0.55\textwidth}
    >{\raggedright\arraybackslash}p{0.34\textwidth}
}
\toprule
\textbf{Check} & \textbf{Result} \\
\midrule
Robot~1 speed verdicts & 12 / 12 correct \\
Robot~2 speed verdicts & 20 / 20 correct \\
Separation transitions & 4 / 4 correct \\
Online violation periods compared with Gazebo & 2 / 2 matched \\
Minimum localized / physical distance & $0.587$~m / $0.623$~m \\
Successful physical goal positions & 4 / 4 within $0.5$~m \\
\bottomrule
\end{tabular}
\caption{Results of E4.}
\label{tab:eval-e4}
\end{table}

\paragraph{Finding.} E4 shows that the same monitor process can keep the two
robot namespaces separate while also checking a rule that needs observations
from both robots. The agreement with Gazebo confirms that the online
separation verdicts reflect the simulated robot motion and agree with the
localized poses.

\section{E5: Distributed Deployment on Three Machines}
\label{sec:eval-e5}

E5 repeats the E4 mission and rules, but changes where the runtime roles run.
The purpose is to check whether the same monitoring setup continues to work
when DDS and MQTT cross physical-machine boundaries and the verifier is
independent of ROS~2.

\paragraph{Deployment.} The Ubuntu workstation runs Gazebo, two Nav2 stacks,
and the mission. A Raspberry Pi running Ubuntu receives the six E4 topics over
DDS, collects them as data records, and publishes them to a Mosquitto broker
on the same Pi. A macOS laptop receives these records over MQTT and runs the
three E4 checks using Homebrew Python. The Pi and workstation use ROS~2
Kilted and the default Fast DDS implementation. The laptop uses a standalone
Python runtime for checking.

The Pi also runs a second verifier as an experimental reference. The Pi and
Mac verifiers subscribe to the same MQTT topic and use the same generated
checking configuration. Comparing their verdict files tests whether both
placements produce the same property results and contributing record
identifiers. The reference process serves only this experimental comparison.
Figure~\ref{fig:eval-e5-deployment} shows the resulting placement across the
three machines.

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{\input{diagrams/e5_deployment.tex}}
\caption{Deployment of E5. The workstation runs the simulated robots and
Nav2, the wired Raspberry Pi collects their ROS~2 topics and hosts the MQTT
broker, and the Mac runs a ROS-independent verifier. The dashed Pi-local
verifier receives the same records only to compare its verdicts with the Mac.}
\label{fig:eval-e5-deployment}
\end{figure}

Figure~\ref{fig:eval-e5-setup} shows the wired Pi and the workstation GUI.
Figures~\ref{fig:eval-e5-first-crossing}
and~\ref{fig:eval-e5-second-crossing} then follow the two crossings; the Mac
terminal gains one verdict entry when the robots become too close and another
when their separation is restored.

\begin{figure}[htbp]
\centering
\begin{minipage}[c]{0.29\textwidth}
\centering
\includegraphics[width=\linewidth,height=0.29\textheight,keepaspectratio]{figures/evaluation/e5_pi_wired.jpg}\\[-1mm]
{\small (a) Raspberry Pi connected to the router by Ethernet.}
\end{minipage}\hfill
\begin{minipage}[c]{0.68\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/evaluation/e5_gui_ready.png}\\[-1mm]
{\small (b) Gazebo and the two RViz windows on the workstation.}
\end{minipage}
\caption{The physical and simulated sides of the E5 setup. The Pi provides
the ROS~2-facing monitor and MQTT broker on the LAN; the workstation displays
the two simulated robots and their namespaced Nav2 views.}
\label{fig:eval-e5-setup}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.78\textwidth]{figures/evaluation/e5_start.jpg}\\[-1mm]
{\small (a) Start of the first leg.}

\smallskip
\begin{minipage}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/evaluation/e5_first_meet.jpg}\\[-1mm]
{\small (b) First minimum-separation violation.}
\end{minipage}\hfill
\begin{minipage}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/evaluation/e5_first_clean.jpg}\\[-1mm]
{\small (c) Separation restored after the first crossing.}
\end{minipage}
\caption{First E5 crossing. The external display shows Gazebo and both RViz
views on the Ubuntu workstation. The foreground Mac runs the remote verifier;
the added verdict line when the robots meet reports the violation, and the
next line reports recovery once their distance again exceeds 1.0~m.}
\label{fig:eval-e5-first-crossing}
\end{figure}

\begin{figure}[htbp]
\centering
\begin{minipage}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/evaluation/e5_second_meet.jpg}\\[-1mm]
{\small (a) Violation during the return crossing.}
\end{minipage}\hfill
\begin{minipage}[t]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/evaluation/e5_second_clean.jpg}\\[-1mm]
{\small (b) Separation restored.}
\end{minipage}
\caption{Second E5 crossing. The robot motion produces the same negative and
positive separation transitions on the return leg, visible as two further
verdict entries in the Mac terminal.}
\label{fig:eval-e5-second-crossing}
\end{figure}

\FloatBarrier

\paragraph{Results.} All four navigation goals succeeded. The Pi collected
3667 records from the six E4 sources, and both verifiers received all 3667
records.

The two speed streams contained 12 and 18 transitions. Every Mac verdict
matched replay of the corresponding Pi command stream. The separation stream
contained two violation periods and two recoveries. All four transitions
matched replay, including both contributing pose-record identifiers. They
also matched the two physical periods below 1.0~m in the Gazebo position log.
The minimum localized and physical separations were $0.547$~m and $0.621$~m.
All four final physical positions were within $0.10$~m of the requested goal.

\begin{table}[htbp]
\centering
\begin{tabular}{
    >{\raggedright\arraybackslash}p{0.57\textwidth}
    >{\raggedright\arraybackslash}p{0.32\textwidth}
}
\toprule
\textbf{Check} & \textbf{Result} \\
\midrule
Robot~1 speed verdicts on Mac & 12 / 12 correct \\
Robot~2 speed verdicts on Mac & 18 / 18 correct \\
Separation transitions on Mac & 4 / 4 correct \\
Pi-reference / Mac verdict streams & 12 / 12, 18 / 18, and 4 / 4 identical \\
MQTT data delivery to Pi reference / Mac & 3667 / 3667; 3667 / 3667 \\
Online violation periods compared with Gazebo & 2 / 2 matched \\
Minimum localized / physical distance & $0.547$~m / $0.621$~m \\
Successful physical goal positions & 4 / 4 within $0.10$~m \\
\bottomrule
\end{tabular}
\caption{Correctness and delivery results of E5.}
\label{tab:eval-e5-correctness}
\end{table}

\paragraph{Finding.} E5 shows that the collector and verifier can run on
different operating systems and processor architectures while preserving the
same verdict stream. Direct DDS discovery between the workstation and Pi
remained available for the full mission, and MQTT delivered every data record
to the Mac. The observed violations and recoveries on the Mac, together with
the identical Pi reference stream, show that physical distribution preserved
the meaning of the E4 checks.

\section{Answer to RQ4}
\label{sec:eval-rq4-answer}

Table~\ref{tab:eval-rq4-summary} summarizes the monitoring needs exercised by
the experiments. Every experiment used a JSON deployment description to
generate its runtime YAML files. The same data-record format and checking
interfaces were used in integrated and split deployments, with synthetic
nodes, one Nav2 robot, two Nav2 robots, and a heterogeneous physical
deployment.

\begin{table}[htbp]
\centering
\begin{tabular}{
    >{\raggedright\arraybackslash}p{0.38\textwidth}
    >{\centering\arraybackslash}p{0.085\textwidth}
    >{\centering\arraybackslash}p{0.085\textwidth}
    >{\centering\arraybackslash}p{0.085\textwidth}
    >{\centering\arraybackslash}p{0.085\textwidth}
    >{\centering\arraybackslash}p{0.085\textwidth}
}
\toprule
\textbf{Monitoring need} & \textbf{E1} & \textbf{E2} & \textbf{E3} &
\textbf{E4} & \textbf{E5} \\
\midrule
Topic observations & \cmark & \cmark & \cmark & \cmark & \cmark \\
Service observations & -- & \cmark & -- & -- & -- \\
Action feedback and status & -- & \cmark & \cmark & -- & -- \\
Integrated checking & \cmark & \cmark & -- & -- & -- \\
Split checking over MQTT & -- & \cmark & \cmark & \cmark & \cmark \\
Generated runtime configuration & \cmark & \cmark & \cmark & \cmark & \cmark \\
More than one observation in a rule & -- & \cmark & \cmark & \cmark & \cmark \\
Multiple robot namespaces & -- & -- & -- & \cmark & \cmark \\
Three physical machines & -- & -- & -- & -- & \cmark \\
\bottomrule
\end{tabular}
\caption{Monitoring needs exercised in E1--E5.}
\label{tab:eval-rq4-summary}
\end{table}

The results answer RQ4 positively within the scope of the prototype. The
infrastructure supports the application-level monitoring needs exercised by
these case studies: topic, service, and action observations; processing before
transport; checking in the monitor or in a separate verifier; correlation of
several observations; and separation of data from multiple robots. The Nav2
experiments show that these capabilities apply to an existing ROS~2
application with its navigation code unchanged. The generated
configurations show that deployment and checking components can be
changed through configuration while retaining the same monitoring core.
E5 additionally shows that the ROS~2-facing part can run on a Pi while the
checking part runs independently on a macOS machine.

The evaluated scope is one local network, up to two simulated robots, and the
topic, service, and action observations selected in
Chapter~\ref{chapter:design}. Within that scope, E1--E5 provide consistent
evidence that the implemented infrastructure supports the monitoring needs
stated in RQ4, from local topic checks to a three-machine Nav2 deployment.
