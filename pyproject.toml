[project]
name = "ROS2-Monitor-Infra"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "paho-mqtt>=2.0",
    "pyyaml>=6.0",
]

[tool.pytest.ini_options]
testpaths = ["test/unit"]
pythonpath = ["monitor"]
addopts = "-ra"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
