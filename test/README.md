# Tests

This directory contains automated project tests only:

- `unit/`: pytest unit and component tests

Reusable processes that support deployment demonstrations belong under
`demo/common/`, not under `test/`.
