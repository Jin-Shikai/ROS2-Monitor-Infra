services:
  mosquitto:
    image: eclipse-mosquitto:2
    volumes:
      - ../deploy_mode2/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro

  robot1:
    build: {context: ../.., dockerfile: Dockerfile}
    depends_on: [mosquitto]
    volumes:
      - ../../monitor:/monitor
      - ../../custom:/monitor/custom
      - ../common:/demo/common:ro
      - ./robot1.yaml:/demo/config.yaml:ro
    command: /bin/bash -lc "source /opt/ros/kilted/setup.bash && python3 /demo/common/robot_simulator.py stationary-origin --ros-args -r __node:=robot1 -r __ns:=/robot1 & python3 /monitor/monitor_node.py -c /demo/config.yaml"

  robot2:
    build: {context: ../.., dockerfile: Dockerfile}
    depends_on: [mosquitto]
    volumes:
      - ../../monitor:/monitor
      - ../../custom:/monitor/custom
      - ../common:/demo/common:ro
      - ./robot2.yaml:/demo/config.yaml:ro
    command: /bin/bash -lc "source /opt/ros/kilted/setup.bash && python3 /demo/common/robot_simulator.py minimum-distance-cycle --ros-args -r __node:=robot2 -r __ns:=/robot2 & python3 /monitor/monitor_node.py -c /demo/config.yaml"

  global_verifier:
    build: {context: ../.., dockerfile: Dockerfile}
    depends_on: [mosquitto]
    volumes:
      - ../../monitor:/monitor
      - ../../custom:/monitor/custom
      - ./verifier.yaml:/demo/verifier.yaml:ro
      - ../../output/multi_robot_global:/output
    command: python3 /monitor/verdict_runner.py -c /demo/verifier.yaml
